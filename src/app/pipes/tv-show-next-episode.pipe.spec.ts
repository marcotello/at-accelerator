import { TvShowNextEpisodePipe } from './tv-show-next-episode.pipe';

describe('TvShowNextEpisodePipe', () => {
  it('create an instance', () => {
    const pipe = new TvShowNextEpisodePipe();
    expect(pipe).toBeTruthy();
  });
});
